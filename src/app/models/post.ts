export class Post {
    constructor(
        public title: string,
        public description: string,
        public tags: string,
        public lat: number,
        public lng: number,
        ) {}
}